using UnityEngine;
using System;
using System.Collections;

public class GameEvents {

    //protected SequenceRequest<O> RequestSequence<I, O>(I data, SequenceRequestHandler<I, O> request, SequenceRequestCallback<O> callback) {
    //    var seq = new SequenceRequest<O>();
    //    if (callback != null) {
    //        seq.OnCallback += callback;
    //    }
    //    request.Raise(this, new SequenceRequestEventArgs<I, O>(data, seq));
    //    return seq;
    //}

}